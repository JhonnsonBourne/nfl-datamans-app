name: E2E Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  e2e-tests:
    name: End-to-End Tests
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./frontend
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium
      
      - name: Build frontend
        run: npm run build
      
      - name: Start preview server
        run: |
          npm run preview &
          sleep 5
      
      - name: Run E2E tests
        run: |
          npx playwright test tests/e2e/ || true
        continue-on-error: true
      
      - name: Run detailed performance tests
        run: |
          npx playwright test tests/e2e/player-stats-performance-detailed.spec.js || true
        continue-on-error: true
      
      - name: Collect performance data
        if: always()
        run: |
          mkdir -p performance-data
          # Copy any performance JSON files generated by tests
          find frontend -name "*performance*.json" -exec cp {} performance-data/ \; || true
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/
          retention-days: 7
      
      - name: Upload performance data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: performance-data
          path: performance-data/
          retention-days: 30

