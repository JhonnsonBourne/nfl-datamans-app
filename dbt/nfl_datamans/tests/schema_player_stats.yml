version: 2

models:
  - name: stg_player_stats
    description: "Staging view over raw_nfl.player_stats from nflverse."
    columns:
      - name: player_id
        description: "Player identifier from nflverse."
        tests:
          - not_null
      - name: season
        description: "Season year."
        tests:
          - not_null
